# DT Guidelines

- DT tests should cover the user-visible workflow.
- Include approval, rejection, rollback, and review loops.
- Include failure paths and retry behavior.
- Tests should be deterministic and easy to run.
